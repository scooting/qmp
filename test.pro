doo: woo = one
else: hoob: woo = two
else: woo = three

# !isEmpty(p):isEqual(p, absolute_path($p)):
# 
# what = hello
# 
# qtConfig(gc_binaries):hoob: MODULE_CONFIG += gc_binaries


Doofer += wao
qtConfig(gc_binaries):hoob: MODULE_CONFIG += gc_binaries
Doofer = wao
Doofer = wao2
#dorf
!qtConfig(gc_binaries)|hurr {
    MODULE_CONFIG += hoberwober
}
equals(QT_ARCH, i386) {
    WOH = waoh
    p = $$clean_path($$1)
    !isEmpty(p):isEqual(p, absolute_path($p)): return(true)
} else: windy:huuuuo {
    WOBER = dober
} else {
    DOBER = dobes
}





load(durrrr)
CMAKE_DISABLED_FEATURES = $$join(QT_DISABLED_FEATURES, "$$escape_expand(\\n)    ")


